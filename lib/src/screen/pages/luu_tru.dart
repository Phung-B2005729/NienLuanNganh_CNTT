import 'package:flutter/material.dart';

class LuuTru extends StatelessWidget {
  const LuuTru({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(child: const Text('Danh sách đọc')),
    );
  }
}
